- Added mode selection and full directory scaffold in `dw start`
- Introduced `role` and `flow` subcommands for role import and workflow inspection
- Updated README and docs to reflect new management commands
